<%= form_for @invite_changeset, Routes.invite_path(@conn, :create),
fn f -> %>
  <%= if @invite_changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :invitee %>
  <%= email_input f, :invitee %>
  <%= error_tag f, :invitee %>

  <div class="mt-2">
    <%= submit "Add attendee", class: "btn btn-primary mr-2" %>
  </div>
<% end %>
